{
    "apps": [
        {
            "name": "casa-app",
            "script": "./node_modules/next/dist/bin/next",
            "exec_mode": "cluster",
            "env": {
                "PORT": "5454"
            }
        }
    ]
}
